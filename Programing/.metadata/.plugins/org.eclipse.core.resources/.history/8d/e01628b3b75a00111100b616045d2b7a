#pragma once

#include "main.h"

//Basic AT commands

#define AT			"AT\r\n"
#define RESET		"AT+RST\r\n"
#define	SLEEP		"AT+SLEEP\r\n"
#define ECHO_ON		"ATE1\r\n"
#define ECHO_OFF	"ATE"

// Допоміжні макроси для перетворення чисел у рядок
#define STR_HELPER(x) #x
#define STR(x) STR_HELPER(x)

//Settings
#define baudrate    115200
#define databits    8
#define stopbits    1
#define parity      0

#define UARTCFG     "AT+UART=" STR(baudrate) "," STR(databits) "," STR(stopbits) "," STR(parity) "\r\n"

//BLE Basic commands

#define BLESTATE	"AT+BLESTATE?\r\n"
#define BLEDISCON	"AT+BLEDISCON\r\n"


// Transparent mode
#define	TRANSPARENT	"+++"
#define TRANSENTER	"AT+TRANSENTER\r\n"

